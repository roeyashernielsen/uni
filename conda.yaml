name: uni
channels:
  - defaults
dependencies:
  - python=3.7
  - pandas
  - scikit-learn
  - jupyter
  - pip
  - pip:
      - pendulum==1.4.4
      - mlflow
      - prefect[dev]
      - apache-airflow
      - tox