name: uni
channels:
  - defaults
dependencies:
  - python=3.7
  - pandas
  - numpy
  - pyarrow
  - pyspark
  - scikit-learn
  - jupyter
  - pip
  - pip:
      - mlflow
      - prefect[dev]
      - apache-airflow
      - pendulum==1.4.4
      - tox